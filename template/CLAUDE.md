# このプロジェクトについて

<!--
このファイルは「何を・どんな構成で作っているか」を Claude に伝えるファイルです。
{{ }} を自分のプロジェクトの情報に置き換えてください。
教え方の方針はここには書きません（TEACHING.md に集約）。
-->

- {{このプロジェクトの一行説明。例: ○○を作りながら△△を学ぶ学習プロジェクト}}
- 目的は {{学習/ポートフォリオ/趣味 など}}。**コードは本人が書くことに価値がある**
- {{フォルダやモジュールの説明。複数フォルダがあるなら、それぞれの役割を1行ずつ}}

## 全体アーキテクチャ

<!-- 図があると Claude が構成を誤解しにくい。無ければこの節ごと消してOK -->

```text
{{ここに構成図やディレクトリの関係を書く。例:
[ブラウザ] → フロント(port 5173) → API(port 3000) → DB
}}
```

- {{知っておいてほしい構成上の事実を箇条書きで}}

## 環境の事実（先生が知らないと誤診断するもの）

<!--
「知らないと Claude がバグと誤診断する“正常な挙動”」や
「このプロジェクト特有の起動・確認手順」をここに書く。
初心者が一番ハマるポイントを先回りで潰せる。例をいくつか置いておく: -->

- {{例: サーバーはコード変更を自動反映しない。変えたら手動で再起動が必要}}
- {{例: 開発モードでは特定の処理が2回走る。それは正常でバグではない}}
- {{例: npm コマンドは ○○/ に cd してから（ルートに package.json は無い 等）}}
- 構文チェック手段: {{例: `node --check file.js` / `python -m py_compile file.py` など}}

# 教え方について

**教え方・口調・応対は、必ず @TEACHING.md に従う。教え方の方針をこのファイルには書かない
（2ファイルに書くと矛盾するため）。CLAUDE.md と TEACHING.md の記述が食い違って見える場合は
TEACHING.md を正とする。**

# 分割ファイル（必ず両方読むこと）

- 教え方・人格・応対の見本・間違い方の傾向 → @TEACHING.md （原則変更しない）
- ロードマップ・現在地 → @PROGRESS.md （毎セッション更新）
